package com.helper.core.ingestion.transaction;

import com.helper.core.ingestion.Entry;

/**
 * Marker interface for transaction entries (bank statements, savings, etc.).
 */
public interface TransactionEntry extends Entry {
}
